# السنوات المالية (Fiscal Years)

## الهدف
التحكم المحاسبي الزمني للنظام — ميزة أنظمة محاسبية قوية.

## الوظائف المنفذة

### 1. إنشاء سنة مالية
- **السنة** (مثال: 2025)
- **تاريخ البداية** / **تاريخ النهاية**
- ملاحظات اختيارية

### 2. حالة السنة
- **🟢 مفتوحة (open)** — السماح بفواتير وقيود
- **🔒 مغلقة (closed)** — منع فواتير وقيود؛ السماح بالعرض والتقارير فقط
- **⚠ مقفلة جزئيًا (partial)** — مقفل حتى تاريخ محدد (`closed_until`)

### 3. قفل السنة
- **إغلاق كامل**: منع فواتير وقيود جديدة للفترة كلها
- **إغلاق جزئي**: تحديد تاريخ «مقفل حتى» — بعد هذا التاريخ لا يُسمح بإدخال
- **إعادة فتح**: تحويل السنة إلى مفتوحة مرة أخرى (للتعديلات الاستثنائية)

### 4. فتح فترة استثنائية
- **من تاريخ → إلى تاريخ** ضمن نطاق السنة المالية
- **الغرض**: استيراد قيود، تصحيح أخطاء
- **سجل تدقيق**: كل فتح استثنائي يُسجَّل في Audit Log

### 5. استيراد قيود يومية (Stub)
- شاشة **استيراد قيود** جاهزة
- المخطط: رفع CSV/Excel → معاينة → التحقق (توازن، تواريخ ضمن النطاق) → اعتماد ونشر
- عند التنفيذ الكامل: تسجيل العملية في سجل تدقيق السنة المالية

### 6. سجل التدقيق (Audit Log)
- كل عملية: إنشاء سنة، إغلاق، إغلاق جزئي، إعادة فتح، فتح فترة استثنائية
- تخزين: المستخدم، التاريخ، تفاصيل (JSON)

## ملاحظات محاسبية
- **لا يسمح بتعديل قيد بعد إغلاق السنة** — يُطبَّق عند ربط التحقق من التاريخ في شاشات القيود/الفواتير (يُضاف لاحقًا في الـ middleware أو في routes).
- **أي فتح استثنائي = سجل تدقيق إلزامي** — منفذ في `FiscalYearAuditLog`.

## الوصول
- من **لوحة التحكم**: بطاقة **السنوات المالية (Fiscal Years)** → قائمة السنوات → إنشاء / تفاصيل / إغلاق / فترة استثنائية.
- المسار: `/fiscal-years/`

## الجداول
- `fiscal_years` — السنوات (year, start_date, end_date, status, closed_until, notes)
- `fiscal_year_exceptional_periods` — الفترات الاستثنائية (fiscal_year_id, start_date, end_date, reason)
- `fiscal_year_audit_log` — سجل التدقيق (fiscal_year_id, action, user_id, details_json)

## ميزة متقدمة (للتوسع لاحقًا)
- **إنشاء عمليات بناءً على القيد**: قيد مبيعات مستورد → إنشاء فواتير تلقائيًا؛ قيد رواتب → إنشاء مسير رواتب. (غير منفذة — يمكن تنفيذها في مرحلة لاحقة.)
