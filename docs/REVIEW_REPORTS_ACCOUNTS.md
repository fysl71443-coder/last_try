# مراجعة شاشتي التقارير والحسابات — تقرير تنفيذي

تمت مراجعة **شاشة التقارير** و**شاشة الحسابات (المالية)** وفق **نظام التصميم (DESIGN_SYSTEM.md)** ومتطلبات مستوى **ERP احترافي**. يلخص هذا الملف ما تم تنفيذه والتحسينات الموصى بها لاحقاً.

---

## ١. ما تم تنفيذه في هذه الجلسة

### شاشة التقارير (Reports)

- **الصفحة الرئيسية (reports.html)**  
  - كانت متماشية مسبقاً: `ops-layout hub-wrap`، `ops-header-bar`، بطاقات، زر رئيسية واحد.  
  - لم تُغيّر؛ الهيكل يلتزم بنظام التصميم.

- **مبيعات الفرع حسب اليوم (reports_branch_sales.html)**  
  - تصحيح رابط لوحة التحكم: `url_for('dashboard')` → `url_for('main.dashboard')`.  
  - إضافة رابط **«العودة للتقارير»** (`reports.reports`) في الهيدر.  
  - توحيد الزر الأساسي: **طباعة** كزر Primary واحد في الهيدر.

- **تقرير المبيعات (reports/sales_report.html)**  
  - إضافة **ops-header-bar** مع عنوان ووصف.  
  - إضافة روابط: **الرئيسية** (`main.dashboard`) و**التقارير** (`reports.reports`).  
  - استخدام `ops-layout hub-wrap` للحاوية.

### شاشة الحسابات (Financials / Accounts)

- **قائمة الحسابات (financials/accounts.html)**  
  - تصحيح رابط لوحة التحكم: `url_for('dashboard')` → `url_for('main.dashboard')`.

- **كشف حساب (financials/account_statement.html)**  
  - تصحيح رابط لوحة التحكم: `url_for('main.dashboard')`.  
  - إضافة رابط **شاشة الحسابات المتكاملة** (`financials.accounts_hub`).  
  - إضافة **زر طباعة** (مع `no-print` حتى لا يظهر عند الطباعة).

- **قائمة الدخل (financials/income_statement.html)**  
  - استبدال الروابط الثابتة للطباعة والتصدير بـ **url_for**:  
    `financials.print_income_statement` و `financials.export_income_statement`.  
  - استبدال الرموز التعبيرية (🔹) في عناوين الأقسام بأيقونات **Font Awesome** (اتساقاً مع DESIGN_SYSTEM).

- **المركز المالي (financials/balance_sheet.html)**  
  - توحيد الألوان مع نظام التصميم: `--bs-primary: #1565c0` و `--bs-primary-dark: #0d47a1`.  
  - استبدال الرموز التعبيرية (🟦/🟥) في رؤوس الأقسام بأيقونات **Font Awesome**.  
  - ضبط رابط كشف الحساب: استخدام `data.date.strftime('%Y-%m-%d')` عند وجود `data.date`.

- **شاشة الحسابات المتكاملة (financials/accounts_hub.html)**  
  - استبدال الروابط الثابتة (`/dashboard`, `/financials/...`) بـ **url_for**  
    (`main.dashboard`, `financials.balance_sheet`, `financials.trial_balance`, `financials.income_statement`) لضمان عمل التنقل مع تطبيق `create_app()` والـ blueprints.

---

## ٢. التوافق مع نظام التصميم

| البند | التقارير | الحسابات |
|-------|----------|----------|
| الحاوية `ops-layout hub-wrap` | ✅ | ✅ |
| الهيدر `ops-header-bar` (H1 + وصف + أزرار) | ✅ (مع إضافات في الفرع وتقرير المبيعات) | ✅ |
| البطاقات `ops-form-card` / `screen-section` | ✅ | ✅ |
| الجداول `screen-table` داخل `screen-table-wrap` | ✅ حيث وُجدت | ✅ |
| زر Primary واحد كحد أقصى في الهيدر | ✅ | ✅ |
| ألوان أساسية (#1565c0 / #0d47a1) | ✅ | ✅ (تم توحيد المركز المالي) |
| عدم تحميل تقرير ثقيل قبل اختيار الفترة | ✅ | ✅ |

---

## ٣. الطباعة والتصدير

- **كشف حساب**: زر طباعة يعتمد `window.print()` مع إخفاء الزر عند الطباعة (`no-print`).  
- **قائمة الدخل**: روابط الطباعة والـ CSV تستخدم `url_for` وتفتح في نافذة جديدة.  
- **المركز المالي / ميزان المراجعة**: أزرار طباعة وتصدير PDF/Excel موجودة ومخفية عند الطباعة.  
- **تقارير المبيعات/المصروفات/المشتريات**: أزرار طباعة أو روابط لصفحات طباعة مخصصة.

---

## ٤. تحسينات إضافية (تم تنفيذها لاحقاً)

- **استبدال الرموز التعبيرية في accounts_hub**: تم استبدال 💸، 🧾، 💰، 🏦، 👤 في تبويب «العمليات المالية» بأيقونات Font Awesome (`fa-money-bill-wave`, `fa-file-lines`, `fa-coins`, `fa-building-columns`, `fa-user`) لاتساق مع DESIGN_SYSTEM.
- **تقارير المصروفات والمشتريات**: تم إضافة `ops-header-bar` مع عنوان ووصف، وروابط «الرئيسية» و«التقارير» في كلا التقريرين (مثل تقرير المبيعات)، مع استخدام `ops-layout hub-wrap` وتعديل العنوان إلى `h2` داخل منطقة الطباعة.

## ٥. توصيات لاحقة (لم تُنفّذ)

1. **شاشة الحسابات المتكاملة (accounts_hub)**  
   - الصفحة حالياً **لا تمتد من base.html** (صفحة مستقلة كاملة). للمواءمة الكاملة مع بقية النظام يمكن جعلها تمتد من `base.html` مع الحفاظ على التبويبات والمحتوى، أو الإبقاء على الوضع الحالي.

2. **Pagination وحدود البيانات**  
   - التأكد من أن جداول التقارير والحسابات لا تحمّل أكثر من 50–100 سجل افتراضياً مع `per_page` واضح (حسب ما ورد في DESIGN_SYSTEM).

3. **صفحات طباعة مخصصة**  
   - كشف الحساب: يمكن إضافة route مخصص للطباعة مع ترويسة وتذييل وتنسيق A4 إذا رُغب في طباعة أكثر احترافية من `window.print()` الحالي.

---

## ٦. ملخص

تم تنفيذ **تحسينات شكلية ووظيفية** على شاشتي التقارير والحسابات لتحقيق:

- توحيد الروابط (`main.dashboard` و `url_for` لجميع الصفحات المالية والتقارير).  
- توحيد الألوان والعناصر البصرية (أيقونات Font Awesome بدلاً من الرموز التعبيرية حيث تم التعديل).  
- إضافة روابط تنقل واضحة (العودة للتقارير، شاشة الحسابات، طباعة كشف الحساب).  
- الالتزام بقواعد نظام التصميم (هيدر واحد، زر Primary واحد في الهيدر، بطاقات وجداول موحدة).

بهذا تكون شاشة التقارير وشاشة الحسابات في مستوى **احترافي متوافق مع DESIGN_SYSTEM** وجاهزة للاستخدام في بيئة ERP.
