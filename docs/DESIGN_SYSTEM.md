# Design System – توحيد التصميم (UI/UX)

النظام محاسبي + إداري (Accounting & POS) لمطعمين (China Town / Place India).  
هذه الوثيقة تحكم **جميع الشاشات ما عدا المبيعات (Sales / POS)**.

---

## ١. الأهداف

- **توحيد التصميم**: نفس الألوان، الخطوط، البطاقات، العناوين، الأزرار، الجداول، النوافذ المنبثقة.
- **لون أساسي**: أزرق (Corporate / Finance).
- **تحسين UX**: لا ازدحام بصري، لا تداخل عناصر، هدف واضح واحد لكل شاشة.

---

## ٢. الألوان

| الاستخدام | القيمة | الملاحظة |
|-----------|--------|----------|
| **عناوين** | `--ops-blue-dark` (#0d47a1) | أزرق داكن |
| **أزرار أساسية** | `--ops-blue` (#1565c0) | أزرق متوسط |
| **خلفيات خفيفة** | `--ops-blue-light` (#e3f2fd) | أزرق فاتح |
| **خلفية الصفحة** | `#f1f5f9` (hub-wrap) | رمادي أزرق فاتح |

عدم الإفراط في ألوان ثانوية. التحذيرات فقط: أخضر (نجاح)، أحمر (خطر)، برتقالي (تنبيه).

---

## ٣. التخطيط الموحد

- **الحاوية**: `ops-layout hub-wrap` داخل `container py-4`.
- **الهيدر**: `ops-header-bar` — عنوان + وصف قصير + أزرار إجراءات فقط.
- **المحتوى**: `ops-body` مع `ops-main`. استخدم `ops-body-no-sidebar` عند عدم وجود شريط جانبي.

**🔹 العناوين (إجباري)**  
كل `ops-header-bar` يجب أن يحتوي:
- **عنوان رئيسي (H1)** — اسم الشاشة.
- **وصف سطر واحد** — يشرح ما الذي يمكن فعله في هذه الشاشة فقط.
- **البطاقات**: `ops-form-card` أو `screen-section`. رأس: `ops-form-head`، جسم: `ops-form-body`.
- **الفلاتر**: `row g-3 align-items-end` داخل البطاقة؛ أزرار `btn btn-primary` مع أيقونة عند الحاجة.

**القيم الموحدة (CSS):**
- `--ops-card-radius`: 12px  
- `--ops-card-shadow`, `--ops-card-border`  
- `--ops-gap`: 1.25rem  
- `--ops-toolbar-gap`: 0.75rem  

---

## ٤. القواعد العامة

- **Card-based layout** في جميع الشاشات.
- كل شاشة: **Header** واضح → **Toolbar** خفيف (بحث، فلترة، أزرار أساسية) → **Content** (جدول / نموذج / بطاقات).
- **جداول**: رؤوس ثابتة، مسافات مريحة، أزرار إجراءات واضحة (عرض، تعديل، حذف). استخدام `screen-table` داخل `screen-table-wrap`.
- **نماذج**: تقسيم منطقي (Sections)، تسميات واضحة، تحقق مرئي.
- **أزرار**: Primary (أزرق)، Secondary (رمادي)، Danger (أحمر، محدود).
- **النوافذ المنبثقة (Modals)**: استخدام class `screen-modal` على `.modal` لرأس أزرق موحد.

**🔹 الأزرار (حد أقصى)**  
- في الـ **Header**: لا يزيد عن **زر Primary واحد**.  
- في **الشاشة ككل**: لا يزيد عن **زر Danger واحد** (مثلاً حذف أو إلغاء نهائي).

---

## ٥. الشاشات حسب النوع

### مالية ومحاسبية (قيود، حسابات، قوائم، VAT، تقارير)

- إبراز: **الفترة**، **الفرع**، **الحالة**.
- **Summary Cards** أعلى الشاشة عند الحاجة.
- تقليل الجداول الثقيلة عند التحميل الأول؛ استخدام فلترة وفترة واضحة.
- تحسين **الطباعة** و**التصدير** (PDF / XLS) ووضوح الأرقام والمحاذاة.

### عملاء / موردون

- **Cards + Table**: بطاقة معلومات سريعة + جدول تفاعلي.
- إجراءات واضحة: كشف حساب، فواتير، مدفوعات.
- عدم تحميل كل البيانات دفعة واحدة (بحث، صفحات، lazy عند الحاجة).

### مخزون

- تبسيط العرض: كميات، تكلفة، تنبيهات.
- ألوان خفيفة للحالة: `stock-low` (تنبيه)، `stock-ok` (عادي).

### تقارير

- **عدم تحميل التقرير تلقائياً**. المستخدم يختار الفترة، الفرع، النوع ثم يتم العرض.
- واجهة نظيفة بدون ازدحام.

### إعدادات

- تقسيم إلى: **Company**، **Accounting**، **VAT**، **Users**، **Tables**.
- استخدام **Tabs** أو **Cards**؛ منع شاشة إعدادات طويلة ومربكة.

---

## ٦. الأداء

- لا استعلامات غير مستخدمة؛ لا تحميل بيانات لا تظهر للمستخدم.
- استخدام **Pagination**، **Lazy loading**، **Cache** عند الحاجة.
- **Dashboard**: تنقل + KPIs فقط؛ بدون استعلامات ثقيلة.

**🔹 تحميل البيانات (توضيح السلوك)**  
- **أي شاشة تحتوي جدولاً**: لا تحميل أكثر من **50–100 سجل افتراضياً**؛ و`per_page` واضح للمستخدم (مثلاً في شريط الفلترة أو التذييل).  
- **أي شاشة مالية**: لا تنفيذ **حسابات تجميعية** (ميزان، قائمة دخل، إلخ) إلا **بعد تحديد الفترة** من المستخدم.

---

## ٧. استثناء إجباري

**ممنوع** تعديل أو إعادة تصميم شاشة **المبيعات (Sales / POS)** — لا UI، لا UX، لا Layout، لا Logic.

---

## ٨. الملفات المرجعية

- `static/css/screens.css`: نظام الألوان، `ops-*`, `screen-*`, `hub-*`, `kpi-*`, `stock-*`.
- `templates/base.html`: هيكل الصفحة، navbar، ترويسة.
- `docs/REDESIGN_PLAN_HUB.md`: تفاصيل الشاشات وقسمة الوظائف.

---

## ٩. البرومبت التنفيذي الموحد (Unified UI/UX Prompt)

يُستخدم كما هو عند طلب توحيد الواجهة أو مراجعة التصميم:

```
🔵 Unified UI/UX Prompt – Accounting System

أنت تعمل على نظام ويب محاسبي + إداري مبني بـ Flask + Jinja2 + Bootstrap 5، يخدم مطعمين (China Town / Place India).

طبّق Design System التالي على جميع الشاشات في النظام مع تحسين UX/UI حسب طبيعة كل شاشة، دون تعقيد أو تداخل، وبشكل احترافي، مع استثناء كامل لشاشة المبيعات (Sales / POS).

القواعد الصارمة
- ممنوع تعديل شاشة Sales / POS نهائياً (UI / UX / Layout / Logic).
- جميع الشاشات الأخرى يجب أن:
  - تستخدم Card-based layout
  - تعتمد اللون الأزرق كلون أساسي
  - تتبع نفس العناوين، الأزرار، الجداول، النماذج

Design System (إجباري)
- الألوان: Primary #1565c0 | Dark Blue (Headers) #0d47a1 | Light Blue (Backgrounds) #e3f2fd | Page background #f1f5f9
- Layout: ops-layout + hub-wrap | ops-header-bar (عنوان + وصف + أزرار فقط) | ops-form-card / screen-section | ops-body-no-sidebar عند عدم الحاجة لقائمة جانبية
- Components: Tables → screen-table داخل screen-table-wrap | Modals → screen-modal برأس أزرق | Buttons: Primary أزرق، Secondary رمادي، Danger أحمر (محدود)

تحسين حسب نوع الشاشة
- محاسبة/مالية: إبراز الفترة، الفرع، الحالة | Summary cards عند الحاجة | لا تحميل بيانات أو حسابات ثقيلة تلقائياً
- عملاء/موردون: بطاقة معلومات + جدول | إجراءات واضحة (كشف حساب، فواتير، مدفوعات)
- مخزون: كميات، تكلفة، تنبيهات | ألوان خفيفة للحالة
- تقارير: لا تقرير يُعرض قبل اختيار الفترة | واجهة نظيفة بدون ازدحام
- إعدادات: Tabs أو Cards | تقسيم منطقي (Company, Accounting, VAT, Users, Tables)

الأداء
- لا استعلامات غير مستخدمة | Pagination افتراضي | Lazy loading عند الحاجة | Dashboard = KPIs + تنقل فقط

النتيجة المطلوبة
نظام محاسبي احترافي، موحّد بصرياً، واضح، سريع، ومنظّم، بدون تعقيد.
```

---

## ١٠. التابع — حالة التطبيق والخطوات التالية

### ما تم تنفيذه (مرجع: REDESIGN_PLAN_HUB.md)
- **CSS:** `hub-wrap`, `hub-tabs`, جداول أزرق داخل hub، `ops-body-no-sidebar`، `ops-form-card`, `screen-section`, `screen-modal`.
- **الشاشات المطبّق عليها:** لوحة التحكم، المصروفات، المشتريات، ميزان المراجعة، قائمة الدخل، المستخدمون، الإعدادات، المنيو، العملاء، الموردون، كل الفواتير، قيود اليومية، التقارير، المدفوعات، المخزون، VAT.

### التحقق من التوحيد (Checklist لكل شاشة جديدة أو مراجعة)
- [ ] الحاوية: `ops-layout hub-wrap` داخل `container py-4`.
- [ ] الهيدر: `ops-header-bar` مع H1 + وصف سطر واحد + أزرار إجراءات فقط (Primary واحد كحد أقصى في الهيدر).
- [ ] البطاقات: `ops-form-card` أو `screen-section`؛ رأس `ops-form-head` / `screen-section-title`، جسم `ops-form-body` / `screen-section-body`.
- [ ] الجداول: `screen-table` داخل `screen-table-wrap`.
- [ ] النوافذ المنبثقة: `screen-modal` على `.modal`.
- [ ] لا تحميل بيانات ثقيلة أو تقارير تلقائياً دون اختيار الفترة/الفلتر.
- [ ] استثناء: لم تُعدّل شاشة المبيعات (Sales / POS).

### تابع — جلسة توحيد إضافية (مرجع)
- **accounts_hub.html:** تحديث لوحة الألوان من Teal إلى Hub Blue (`--ah-primary: #0d47a1`, `--ah-primary-light: #e3f2fd`) مع إضافة رابط "لوحة التحكم" في الهيدر.
- **account_statement.html:** تطبيق `ops-layout hub-wrap`، `ops-header-bar` (H1 + وصف)، `ops-form-card` للفترة، `screen-section` + `screen-table` للجدول، ورابط Dashboard.
- **operations.html:** تحويل الصفحة لتمتد من `base.html` مع `ops-layout hub-wrap`، `ops-header-bar`، `ops-form-card`، وروابط لوحة التحكم وشاشة الحسابات؛ نقل السكربت إلى `{% block scripts %}`.
- **financial_dashboard.html:** تطبيق `ops-layout hub-wrap`، `ops-header-bar`، فلتر الفترة في `ops-form-card`، تحويل البطاقات إلى `ops-form-card` مع `ops-form-head`، الجداول إلى `screen-table` داخل `screen-table-wrap`، وبطاقات الملخص إلى `kpi-value`/`kpi-row`.
- **reports_monthly.html:** تحديث ألوان CSS إلى Hub Blue (`--p1: #0d47a1`, `--p2: #1565c0`)، وإضافة `ops-layout hub-wrap` و`ops-header-bar` للحاوية والشريط العلوي.
- **chart.html (شجرة الحسابات):** تطبيق `ops-layout hub-wrap`، `ops-header-bar` (H1 + وصف)، زر Primary واحد ("إضافة حساب جديد")، البطاقات إلى `ops-form-card`، النوافذ المنبثقة إلى `screen-modal`، وأزرار الحفظ إلى `btn-primary`.

### تابع — جلسة توحيد الشاشات المتبقية (مرجع)
- **dashboard.html:** إضافة `ops-layout hub-wrap` و`ops-header-bar` (H1 + وصف) فوق لوحة البطاقات.
- **all_expenses.html:** توحيد كامل: `ops-layout hub-wrap`، `ops-header-bar`، `ops-form-card`، `screen-table`.
- **invoice_view.html:** توحيد كامل: `ops-header-bar`، `ops-form-card`، `screen-section` + `screen-table`، أزرار Primary للطباعة والدفع.
- **supplier_edit.html، suppliers_edit.html:** توحيد كامل مع إصلاح HTML في supplier_edit؛ `ops-form-card` للنماذج.
- **expenses_edit.html، expenses_view.html:** توحيد كامل: `ops-header-bar`، `ops-form-card`، `screen-section`/`screen-table`، أزرار موحدة.
- **table_manager.html:** توحيد: `ops-layout hub-wrap`، `ops-header-bar`، `ops-form-card` لإضافة القسم.
- **financials/backfill.html، backfill_result.html، accounts.html:** توحيد كامل مع `ops-header-bar` و`ops-form-card` و`screen-table`.
- **inventory_intelligence.html:** إضافة `ops-header-bar` و`ops-layout hub-wrap` وتحديث ألوان CSS إلى Hub Blue.
- **reports_branch_sales.html، report_branch_sales.html:** توحيد مع `ops-header-bar` و`ops-form-card` و`screen-table`.
- **reports_print_expenses.html:** إضافة `ops-layout hub-wrap` و`screen-table`؛ الإبقاء على طباعة كما هي.
- **menu_simple.html:** توحيد الهيدر والبطاقات و`screen-table`؛ إصلاح علامات HTML الزائدة في النهاية.

### مراجعة شاملة — تنفيذ خطة المراجعة (مرجع: REVIEW_PLAN.md)
- **خطة مراجعة:** إنشاء `docs/REVIEW_PLAN.md` — منهجية مراجعة (جرد شاشات، Checklist، أيقونات، روابط، طباعة).
- **أيقونات:** استبدال الرموز التعبيرية بأيقونات Font Awesome في `inventory.html`، `inventory_intelligence.html`، `expenses.html`، `payments.html`، `raw_materials.html`، `reports_monthly.html`؛ توحيد التحذيرات بـ `fa-triangle-exclamation`.
- **أزرار:** توحيد زر Dashboard `btn-outline-secondary` في `inventory`، `menu`؛ استبدال `btn-success` بـ `btn-outline-primary` لاستيراد الوجبات في `menu.html`.
- **قوالب طباعة رسمية:**
  - `journal_print.html`: ترويسة (شركة)، عنوان تقرير، @page A4، تذييل بتاريخ الطباعة، زر طباعة.
  - `journal_print_all.html`: إزالة emoji، ترويسة، تذييل، @page.
  - `vat_print_fallback.html`: ترويسة وتذييل، @page، حدود وجداول أوضح.
  - `report_branch_sales_print.html`: ترويسة، عنوان، تذييل، إرشاد لاستخدام شاشة «مبيعات الفرع حسب اليوم» للتفاصيل.
  - `report_template.html`: إزالة emoji، ترويسة، تذييل بتاريخ الطباعة، @page.
  - `reports_print_expenses`، `reports_print_purchases`: ربط Dashboard، `@page` A4، تحسين الترويسة.

### الخطوات التالية (تابع)
1. **مراجعة بصريّة:** المرور على الشاشات المذكورة أعلاه والتأكد من تطابقها مع الألوان والتخطيط في هذا المستند.
2. **Pagination وحدود البيانات:** التأكد من أن الجداول لا تحمّل أكثر من 50–100 سجل افتراضياً مع `per_page` واضح.
3. **التقارير والشاشات المالية:** التأكد من عدم تنفيذ حسابات تجميعية إلا بعد تحديد الفترة من المستخدم.
4. **إعدادات:** التأكد من تقسيم الإعدادات إلى Tabs أو Cards (Company, Accounting, VAT, Users, Tables) دون شاشة واحدة طويلة.
