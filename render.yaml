services:
  - type: web
    name: مطعم_القرية_الصيني
    env: python
    plan: free
    region: frankfurt
    buildCommand: |
      pyenv install -s 3.11.7
      pyenv global 3.11.7
      pip install -r requirements.txt
    startCommand: gunicorn -k gevent app:app
